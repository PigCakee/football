<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorCarbonBlack</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorLightBlack</item>
        <item name="android:textColor">@color/colorMilkWhite</item>
        <item name="android:colorBackground">@color/colorLightBlack</item>
        <item name="fontFamily">@font/muller_bold</item>
        <item name="android:textSize">16sp</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MyApplication.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="titleTextStyle">
        <item name="android:textSize">32sp</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="Theme.MyApplication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>